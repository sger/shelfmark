POSTGRES_DB=library
POSTGRES_USER=library
POSTGRES_PASSWORD=library
POSTGRES_PORT=5432
DATABASE_URL=postgres://library:library@localhost:5432/library
# REQUIRED: the app refuses to start if this is unset, empty, or left as a
# placeholder. Generate a strong value with: openssl rand -hex 32
JWT_SECRET=
STORAGE_DIR=./storage/data
ALLOWED_ORIGINS=http://localhost:5173
BIND_ADDR=0.0.0.0:6060
BACKEND_PORT=6060
FRONTEND_PORT=5173
